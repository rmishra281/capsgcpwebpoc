apiVersion: apps/v1
kind: Deployment
metadata:
  name: capsgcpwebpoc
spec:
  selector:
    matchLabels:
      app: capsgcpwebpoc
  replicas: 2
  template:
    metadata:
      labels:
        app: capsgcpwebpoc
    spec:
      containers:
        - name: capsgcpwebpoc
          image: IMAGE
          ports:
            - containerPort: 80
          volumeMounts:
            - name: kaniko-secret
              mountPath: /tmp/test2
          env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /tmp/test2/kaniko-secret.json
      volumes:
        - name: kaniko-secret
          secret:
              secretName: capsgcp2
              defaultMode: 0666
      